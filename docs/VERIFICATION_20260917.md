# 2026年9月17日の掲載コード確認

今回のGitHub整理では、保存した研究結果を再学習して置き換えることはしていません。現在のクラウド運転のコード・モデル・状態も変更していません。

## 掲載コピーで実行した検査

Python 3.13のローカル環境で以下を確認しました。

| 対象 | 結果 | 検査する範囲 |
|---|---|---|
| paper_trading | 29成功、1 skip | 再起動時の重複防止、次の観測での約定、停止・復旧、データ品質、費用、積立計算 |
| 複数通貨の研究コード | 5成功 | 売買価格、将来データを特徴量へ混ぜないこと、非重複、損失と取引0件 |
| 72候補探索コード | 3成功 | 他通貨特徴量の因果性、費用と非重複、週末と実際の欠損 |
| Python構文 | 成功 | 追加した仮想売買・研究コードのcompileall |

学習済みモデルを含めていないため、モデル推論1件は明示的にskipします。これはモデル動作の合格ではありません。元のモデル付き環境とEC2では、配備時に30件すべて成功した記録があります。

GitHub Actionsに新しい専用ワークフローを追加しました。実行結果はActionsで確認できます。既存の研究コードとCIは維持しています。

## 保存結果と現在の検査を分ける

過去の再現300取引、元環境の特徴量500行×50項目の一致、学習・予測再現の結果は、それぞれの実施時点の記録です。今回の軽量テストが学習全体、配信品質、将来の利益を保証するものではありません。

公開対象は報告・集計・参照コードです。APIキー、クラウド接続先識別子、署名付きURL、価格の生データ、学習済みモデル、運転中のSQLite DBは対象から除外しました。旧ファイルを一括コピーせず、リモートの既存ファイルを保った追加更新にしています。
